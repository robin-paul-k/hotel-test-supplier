package com.emimol.hotel.constants;

public enum ErrorCode {
	UNAUTHORIZED,
	FORBIDDEN,
	METHOD_NOT_ALLOWED,
    VALIDATION_FAILED,
    RESOURCE_NOT_FOUND,
    BAD_REQUEST,
    REQUEST_TIMEOUT,
    PARSING_ERROR,
    INTERNAL_ERROR,
}
